# Google Places API (New) — usada para descoberta de profissionais e empresas.
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACES_LANGUAGE=pt-BR
GOOGLE_PLACES_REGION=BR

# Opcional: personalize os campos solicitados para controlar custo e dados retornados.
# GOOGLE_PLACES_FIELDS=places.id,places.displayName,places.formattedAddress,places.location,places.primaryType,places.types,places.websiteUri,places.nationalPhoneNumber,places.businessStatus,places.googleMapsUri,places.rating,places.userRatingCount

# A IA será configurada dentro da aplicação local para uso com OmniRoute.
# Nenhuma variável de IA é necessária nesta etapa.
